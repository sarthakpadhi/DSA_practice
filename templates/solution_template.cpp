/*
 * <Number>. <Title>
 * Link: <url>
 * Difficulty: Easy | Medium | Hard
 * Time: O(?)   Space: O(?)
 */
#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    // replace with actual signature
    int solve(vector<int>& nums) {
        return 0;
    }
};

int main() {
    Solution s;
    vector<int> v = {1, 2, 3};
    assert(s.solve(v) == 6);
    cout << "ok\n";
    return 0;
}
